on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Get yamllint version
        run: yamllint --version

      - name: Lint yaml files
        run: yamllint Exampe_Quiz
